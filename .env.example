# Copy to .env and fill in for local runs: cp .env.example .env
# On Render, set these under the service's Environment tab instead —
# do not commit a real .env file.

ANTHROPIC_API_KEY=
SUPABASE_URL=
SUPABASE_KEY=
